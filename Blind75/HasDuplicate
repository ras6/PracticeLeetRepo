# Time complexity: O ( n ) 
# Space complexity: O ( n ) 
def hasDuplicate(self, nums: List[int]) -> bool:
        seen = set()
        for num in nums:
            if num in seen:
                return True
            seen.add(num)
        return False

'''
Same solution speed in one line :
def hasDuplicate(self, nums: List[int]) -> bool:
  return len(set(nums) < len(nums)

'''
